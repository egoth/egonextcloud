{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Xdebug - Remote SSH",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "pathMappings": {
        "/var/www/html/custom_apps": "${workspaceFolder}/apps",
        "/var/www/html": "${workspaceFolder}/html"
      }
    }
  ]
}
